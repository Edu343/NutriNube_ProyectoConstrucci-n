name: Build and Sonar Analysis
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch: 

jobs:
  sonarqube:
    name: SonarQube Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      - name: Compile Main Code
        run: |
          mkdir -p bin
          find src -name "*.java" > sources.txt
          javac -cp "lib/*" -d bin @sources.txt
          
      - name: Compile Unit Tests
        run: |
          find test -name "*.java" > test_sources.txt
          javac -cp "lib/*:bin" -d bin @test_sources.txt

      - name: Run Tests with JaCoCo
        run: |
          mkdir -p target
          
          JACOCO_AGENT=$(find lib -name "jacocoagent-*.jar" -print -quit)
          JUNIT_LAUNCHER=$(find lib -name "junit-platform-console-standalone-*.jar" -print -quit)

          if [ -z "$JACOCO_AGENT" ] || [ -z "$JUNIT_LAUNCHER" ]; then
            echo "ERROR: Missing JUnit Launcher or JaCoCo Agent JAR in 'lib/'."
            exit 1
          fi

          java -jar "$JUNIT_LAUNCHER" \
            --class-path bin:lib/* \
            --scan-classpath \
            --include-classname-pattern ".*Test" \
            --disable-banner \
            -Djacoco.destfile=target/jacoco.exec \
            -javaagent:"$JACOCO_AGENT"=destfile=target/jacoco.exec,includes=Modelo.*:Controlador.*:Vista.*:Servicios.*
          
      - name: Generate JaCoCo XML Report
        run: |
          mkdir -p target/site/jacoco
          JACOCO_CLI=$(find lib -name "jacococli-*.jar" -print -quit)

          if [ -z "$JACOCO_CLI" ]; then
            echo "ERROR: Missing jacococli JAR in 'lib/'."
            exit 1
          fi

          java -jar "$JACOCO_CLI" report target/jacoco.exec \
            --classfiles bin \
            --sourcefiles src \
            --html target/site/jacoco \
            --xml target/site/jacoco/jacoco.xml
          
      - name: SonarQube Scan
        uses: SonarSource/sonarcloud-github-action@v2.1.1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}